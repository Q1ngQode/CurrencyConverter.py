import requests

from_currency = str(
    input("Enter in the currency you'd like to convert to: ")).upper()

to_currency = str(
    input("Enter in the currency you'd like to convert to: ")).upper()

amount = 


